package people.songpagu.goodgame.api.domain.guild

object GuildControllerPath {
    object Command {
        const val createGuild: String = "/api/v1/guild"
    }

    object Query {
        const val findGuilds: String = "/api/v1/guilds"
    }

}
